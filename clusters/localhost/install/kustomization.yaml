apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: wac-hospital  # chceme tieto objekty umiestniť do `wac-hospital` namespace
                         # tento zapis preťaží hodnoty "namespace" vo všetkých objektoch
labels:
- pairs:
    app.kubernetes.io/part-of: wac-hospital

resources:
- ../../../apps/lm1234-ambulance-ufe
- ../../../apps/lm1234-ambulance-webapi

patches:
- path: patches/ambulance-webapi.service.yaml

components:
- ../../../components/version-developers
- https://github.com/lukasmichalcak/ambulance-webapi//deployments/kustomize/components/mongodb